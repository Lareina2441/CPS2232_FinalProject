1. 先运行reset，读取数据。后续如果想要重置一切记录，就再运行一遍，重新输入记录
2. 再运行menu，开始与用户交互
3. 用户需要先登录或注册。登录：用户先输入自己的名字，如果该name已经存在就输入密码，密码不对就重登。注册：name不可以和已有的重复。（在csv中遍历）
3. 关于priority的应用：用制造商和型号决定某种型号的数量，如果该船已经没有了，那么就让他排队（把不同的client放入priority queue），client可以写入文件。  

